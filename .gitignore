build/bin
node_modules
frontend/dist

# Binarios de build (el output de wails vive en build/bin)
*.exe

# Assets vendorizados (se regeneran en postinstall desde node_modules)
frontend/public/vs
frontend/public/fa

# Backups rotativos de predefined_functions (dev)
frontend/backups/

# Base de átomos (bbolt). Es el almacén VIVO y local de cada máquina; lo que se
# versiona es su export, frontend/predefined_functions.json. Ver store.go.
atoms.db
